module('Component - page-text', {
    setup: function () {
        this.scope = Crocodoc.getScopeForTest(this);
        this.component = Crocodoc.getComponentForTest('page-text', this.scope);
    }
});

/*test('', function () {

});
*/
